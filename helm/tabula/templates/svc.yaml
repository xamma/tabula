apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-svc
  labels:
    app: tabula-depl
  {{- include "tabula.labels" . | nindent 4 }}
spec:
  type: {{ .Values.svc.type }}
  selector:
    app: tabula-depl
    {{- include "tabula.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.svc.ports | toYaml | nindent 2 }}